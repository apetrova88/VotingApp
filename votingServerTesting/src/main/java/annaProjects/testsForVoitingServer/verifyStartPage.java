package annaProjects.testsForVoitingServer;

//import annaProjects.pageObjectsForVoitingServer.StartPage;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import java.util.concurrent.TimeUnit;

public class verifyStartPage {
   /* public static void main (String[] args){
        System.setProperty("webdriver.chrome.driver", "C:/Users/apetrova/Documents/chromedriver_win32/chromedriver.exe");
        WebDriver driver = new ChromeDriver();
        driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS);

        //first test for verification - start page was loaded successfully
        StartPage start_page = new StartPage();
        start_page.getStartPage(driver,"https://exonum.com/demo/voting", "voting app");

        //second test
        start_page.getAppHeader(driver);
       // Assert.assertTrue(getAppHeader.appHeaderText.toLowerCase().equals("e-voting"));

        driver.close();
    }*/
}
